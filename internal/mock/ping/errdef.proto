syntax = "proto3";

package ping;

import "ellie/errors/errors.proto";

option go_package = "github.com/dizzrt/ellie/internal/mock/ping";


enum Errors {
    // option(ellie.errors.default_status) = ErrorStatus_OK;
        
    UNKNOWN = 0 [(ellie.errors.code) = 0];

    Success = 100000 [(ellie.errors.code) = 100000 , (ellie.errors.status) = ERROR_STATUS_PERMISSION_DENIED];
    InvalidParams = 100001 [(ellie.errors.code) = 100001];
} 
